
# Usage Guide

1. Install Dependencies
```bash
pip install -r requirements.txt

2. Run Streamlit App

streamlit run app.py

3. Interact with the Chat

Provide details when prompted.

Provide tech stack as comma-separated values (e.g., Python, Django, PostgreSQL).

Say 'exit' to finish and save your responses.

4. Run Tests

pytest -

5. Generate PDF Report (Optional)

python scripts/generate_report.py


---

5. `docs/report.pdf`
- ✅ Keep as placeholder.
- 🔹 Optional: Use ReportLab to auto-generate a report summarizing collected candidate data, prompts, and question generation.

---

This `docs/` folder is now fully **corrected and ready**.  

---

If you want, I can also **give you the complete fixed `utils/` folder** and everything else ready so your whole project is fully runnable without errors.  

Do you want me to do that next?

